services:
  - type: web
    name: flynn-sdr-portfolio
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
